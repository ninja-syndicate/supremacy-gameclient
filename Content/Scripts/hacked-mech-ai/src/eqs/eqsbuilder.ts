/*
import { EQSArgument, EQSQueryType } from "enums"

interface EQSBuilder {
    setQueryType: (type: EQSQueryType) => EQSBuilder,
    setQueryArgument: (argument: EQSArgument, ) => EQSBuilder,
    build: () => Task
}
export const EqsBuilder = (): 
*/